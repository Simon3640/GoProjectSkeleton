meta {
  name: CreateUserToUpdate
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/user-password
  body: json
  auth: bearer
}

headers {
  Accept-Language: es-ES
}

auth:bearer {
  token:
}

body:json {
  {
    "email": "{{userToUpdateEmail}}",
    "name": "User To Update",
    "otpLogin": false,
    "password": "{{strongPassword}}",
    "phone": "{{userToUpdatePhone}}",
    "role_id": 2
  }
}

script:post-response {
  const response = res.getBody();

  test("Status code is 201", function () {
      expect(res.getStatus()).to.equal(201);
  });

  test("User ID is present and is a number", function () {
      expect(response.data.id).to.exist;
      expect(response.data.id).to.be.a('number');
  });

  bru.setEnvVar("userToUpdateId", response.data.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
