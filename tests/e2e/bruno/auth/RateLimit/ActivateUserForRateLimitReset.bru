meta {
  name: ActivateUserForRateLimitReset
  type: http
  seq: 15
}

post {
  url: {{baseURL}}/user/activate
  body: json
  auth: inherit
}

body:json {
  {
      "token": "{{rateLimitResetActivateToken}}"
  }
}

script:post-response {
  test("Response status code is 200", function () {
      expect(res.getStatus()).to.eql(200);
  });
}

settings {
  encodeUrl: true
}
